apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: pgdb:5432
    database: dataplat
    user: dbadmn
    secureJsonData:
      password: FOO
    isDefault: true
    jsonData:
      sslmode: disable